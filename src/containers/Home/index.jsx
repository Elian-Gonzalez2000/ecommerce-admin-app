import React from "react";
import Layout from "../../components/Layout";
import "./style.css";

const Home = (props) => {
   return (
      <Layout sidebar>
         <h1>Welcome Home</h1>
      </Layout>
   );
};

export default Home;
