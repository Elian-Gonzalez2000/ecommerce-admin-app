import React from "react";
import Layout from "../../components/Layout";

const Home = (props) => {
   return (
      <Layout>
         <h1>Welcome Home</h1>
      </Layout>
   );
};

export default Home;
